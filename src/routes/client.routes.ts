import { Router } from 'express';

const clientRoutes = Router();

clientRoutes.post('');
clientRoutes.get('');
clientRoutes.get('');
clientRoutes.patch('');
clientRoutes.delete('');

export default clientRoutes;
